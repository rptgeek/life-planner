---
phase: 04-goals-and-values-task-reporting-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/GoalReportCard.tsx
  - src/components/ForgottenGoalsCallout.tsx
  - src/components/UnlinkedTasksSection.tsx
  - src/components/TrendChartsSection.tsx
autonomous: true
requirements:
  - RPT-03
  - RPT-04
  - RPT-05
  - RPT-06
must_haves:
  truths:
    - "GoalReportCard renders goal title, role/category badges, open/completed/overdue counts, progress bar, and completion %"
    - "ForgottenGoalsCallout renders amber callout listing goals with zero tasks, hidden when none"
    - "UnlinkedTasksSection renders collapsible accordion of tasks with no goal"
    - "TrendChartsSection renders a Recharts LineChart with one line per goal showing weekly completions"
  artifacts:
    - path: "src/components/GoalReportCard.tsx"
      provides: "Per-goal metrics card"
      contains: "GoalReportCard"
    - path: "src/components/ForgottenGoalsCallout.tsx"
      provides: "Amber callout for taskless goals"
      contains: "ForgottenGoalsCallout"
    - path: "src/components/UnlinkedTasksSection.tsx"
      provides: "Collapsible orphan tasks list"
      contains: "UnlinkedTasksSection"
    - path: "src/components/TrendChartsSection.tsx"
      provides: "Recharts weekly trend chart"
      contains: "TrendChartsSection"
  key_links:
    - from: "src/components/GoalReportCard.tsx"
      to: "src/lib/types.ts"
      via: "GoalReport type import"
      pattern: "import.*GoalReport"
    - from: "src/components/TrendChartsSection.tsx"
      to: "recharts"
      via: "LineChart, ResponsiveContainer imports"
      pattern: "import.*recharts"
---

<objective>
Build the four UI components for the reports dashboard: GoalReportCard, ForgottenGoalsCallout, UnlinkedTasksSection, and TrendChartsSection. All receive data as props (no data fetching).

Purpose: These are the building blocks assembled by the /reports page in Wave 3.
Output: Four component files in src/components/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-goals-and-values-task-reporting-dashboard/04-CONTEXT.md
@.planning/phases/04-goals-and-values-task-reporting-dashboard/04-UI-SPEC.md
@src/lib/types.ts
@src/app/goals/page.tsx
@AGENTS.md

<interfaces>
From src/lib/types.ts (after Plan 01):
```typescript
export interface GoalReport {
  goal: Goal
  openCount: number
  completedCount: number
  overdueCount: number
  totalCount: number
  completionRate: number
}

export interface TrendDataPoint {
  weekLabel: string
  weekStart: string
  [goalTitle: string]: number | string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GoalReportCard and ForgottenGoalsCallout</name>
  <files>src/components/GoalReportCard.tsx, src/components/ForgottenGoalsCallout.tsx</files>
  <read_first>src/lib/types.ts, .planning/phases/04-goals-and-values-task-reporting-dashboard/04-UI-SPEC.md, src/app/goals/page.tsx, AGENTS.md</read_first>
  <action>
**GoalReportCard.tsx** — `'use client'` component. Props: `{ report: GoalReport }`.

Per UI-SPEC Interaction Contract > Goal Card:
- White card: `bg-white border border-slate-200 rounded-lg p-4 shadow-sm`
- Goal title: `text-xl font-semibold text-slate-800` (Heading 20px)
- Role badge (if goal.role): `text-xs font-semibold px-2 py-0.5 rounded-full` with `style={{ backgroundColor: goal.role.color + '20', color: goal.role.color }}`
- Category badge (if goal.category): `text-xs text-slate-500`
- Metric row: "Open: N  Completed: N  Overdue: N" — overdue renders in `text-red-600 font-semibold` only when N > 0, otherwise `text-slate-600`
- Progress bar: `<div className="w-full h-2 bg-slate-200 rounded-full mt-3"><div className="h-2 bg-indigo-500 rounded-full" style={{ width: `${report.completionRate}%` }} /></div>`
- Completion %: `text-indigo-600 text-sm font-semibold mt-1` showing `${report.completionRate}% complete`

**ForgottenGoalsCallout.tsx** — `'use client'` component. Props: `{ goals: Goal[] }`.

Per UI-SPEC Interaction Contract > Forgotten Goals Callout:
- Return null if goals.length === 0
- `border-l-4 border-amber-400 bg-amber-50 p-4 rounded-r-lg`
- Heading: `text-amber-800 font-semibold text-sm` — "Goals without tasks ({goals.length})"
- Goal titles listed inline comma-separated: `text-amber-700 text-sm`
  </action>
  <verify>
    <automated>grep -q "GoalReportCard" src/components/GoalReportCard.tsx && grep -q "ForgottenGoalsCallout" src/components/ForgottenGoalsCallout.tsx && grep -q "border-amber-400" src/components/ForgottenGoalsCallout.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "export default function GoalReportCard" src/components/GoalReportCard.tsx returns a match
    - grep "bg-indigo-500" src/components/GoalReportCard.tsx returns a match (progress bar)
    - grep "completionRate" src/components/GoalReportCard.tsx returns a match
    - grep "export default function ForgottenGoalsCallout" src/components/ForgottenGoalsCallout.tsx returns a match
    - grep "border-amber-400" src/components/ForgottenGoalsCallout.tsx returns a match
    - grep "Goals without tasks" src/components/ForgottenGoalsCallout.tsx returns a match
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>GoalReportCard renders per-goal metrics with progress bar per UI-SPEC. ForgottenGoalsCallout renders amber callout with comma-separated goal titles, hidden when empty.</done>
</task>

<task type="auto">
  <name>Task 2: Create UnlinkedTasksSection and TrendChartsSection</name>
  <files>src/components/UnlinkedTasksSection.tsx, src/components/TrendChartsSection.tsx</files>
  <read_first>src/lib/types.ts, .planning/phases/04-goals-and-values-task-reporting-dashboard/04-UI-SPEC.md, AGENTS.md</read_first>
  <action>
**UnlinkedTasksSection.tsx** — `'use client'` component. Props: `{ tasks: Task[] }`.

Per UI-SPEC Interaction Contract > Unlinked Tasks Section:
- Return null if tasks.length === 0 (hidden entirely per copywriting contract)
- Collapsible accordion with `useState<boolean>(false)` for expanded state
- Header: "Unlinked Tasks ({tasks.length})" — always visible, clickable
- Toggle button uses `ChevronDown` / `ChevronRight` from lucide-react
- Toggle button: `aria-label="Expand unlinked tasks"` when collapsed, `aria-label="Collapse unlinked tasks"` when expanded
- When expanded: flat list of task titles, each `text-sm text-slate-700` on its own row

**TrendChartsSection.tsx** — `'use client'` component. Props: `{ data: TrendDataPoint[], goalTitles: string[] }`.

Per UI-SPEC Interaction Contract > Trend Charts Section:
- Show empty state text ("Not enough data to show trends yet. Completed tasks will appear here as you work.") if data.length === 0
- Section heading: "Completion Trends" in `text-xl font-semibold text-slate-800`
- Import `ResponsiveContainer, LineChart, Line, XAxis, YAxis, Tooltip, CartesianGrid` from `recharts`
- `ResponsiveContainer` width="100%" height={280}
- `LineChart` with data prop
- `XAxis dataKey="weekLabel"`, `YAxis` (integer ticks, no decimals via `allowDecimals={false}`)
- One `Line` per goalTitle: `type="monotone"`, `dataKey={goalTitle}`, `stroke="#6366f1"` with opacity stepped (1.0, 0.7, 0.5, 0.3 cycling for goals beyond 4)
- `Tooltip` showing goal title + count
- `CartesianGrid strokeDasharray="3 3"`
  </action>
  <verify>
    <automated>grep -q "UnlinkedTasksSection" src/components/UnlinkedTasksSection.tsx && grep -q "ResponsiveContainer" src/components/TrendChartsSection.tsx && grep -q "aria-label" src/components/UnlinkedTasksSection.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "export default function UnlinkedTasksSection" src/components/UnlinkedTasksSection.tsx returns a match
    - grep "aria-label" src/components/UnlinkedTasksSection.tsx returns a match
    - grep "ChevronDown\|ChevronRight" src/components/UnlinkedTasksSection.tsx returns a match
    - grep "export default function TrendChartsSection" src/components/TrendChartsSection.tsx returns a match
    - grep "ResponsiveContainer" src/components/TrendChartsSection.tsx returns a match
    - grep "LineChart" src/components/TrendChartsSection.tsx returns a match
    - grep "6366f1" src/components/TrendChartsSection.tsx returns a match (indigo accent)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>UnlinkedTasksSection renders collapsible accordion with accessible toggle. TrendChartsSection renders Recharts LineChart with per-goal weekly trend lines in indigo shades.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- All 4 component files exist and export default functions
- UI-SPEC color tokens used: indigo-500 progress bar, amber-400 forgotten callout, red-600 overdue
</verification>

<success_criteria>
- 4 component files created in src/components/
- Each accepts typed props (GoalReport, Goal[], Task[], TrendDataPoint[])
- Tailwind classes match UI-SPEC exactly
- Recharts imported and used in TrendChartsSection
- Accessible aria-labels on UnlinkedTasksSection toggle
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-goals-and-values-task-reporting-dashboard/04-02-SUMMARY.md`
</output>
