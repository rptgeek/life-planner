---
phase: 01-calendar-preferences
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/CalendarPreferencesPanel.tsx
  - src/app/settings/page.tsx
  - src/components/DailyTimeLog.tsx
autonomous: false
requirements:
  - CAL-01
  - CAL-03
  - CAL-04
user_setup:
  - service: supabase
    why: "Apply migration adding calendar preference columns"
    dashboard_config:
      - task: "Run SQL from supabase/add_calendar_preferences.sql in the Supabase SQL Editor"
        location: "Supabase Dashboard -> SQL Editor"
must_haves:
  truths:
    - "User sees a multi-select list of their Google Calendars after connecting"
    - "User can designate one calendar as the default push target"
    - "User can edit calendar preferences from Settings page"
    - "Selections are saved to Supabase and survive page reload"
  artifacts:
    - path: "src/components/CalendarPreferencesPanel.tsx"
      provides: "Reusable calendar picker with checkboxes and radio buttons"
      min_lines: 60
    - path: "src/app/settings/page.tsx"
      provides: "Settings page with Google Calendar section"
      contains: "CalendarPreferencesPanel"
    - path: "src/components/DailyTimeLog.tsx"
      provides: "Post-connect prompt when no preferences configured"
      contains: "selected_calendar_ids"
  key_links:
    - from: "src/components/CalendarPreferencesPanel.tsx"
      to: "src/lib/useCalendarPreferences.ts"
      via: "hook consumption"
      pattern: "useCalendarPreferences"
    - from: "src/components/CalendarPreferencesPanel.tsx"
      to: "src/lib/googleCalendar.ts"
      via: "listCalendars call"
      pattern: "listCalendars"
    - from: "src/app/settings/page.tsx"
      to: "src/components/CalendarPreferencesPanel.tsx"
      via: "component import"
      pattern: "import CalendarPreferencesPanel"
---

<objective>
Build the calendar preferences UI: a reusable CalendarPreferencesPanel component, integrate it into the Settings page, and add a post-connect setup prompt in DailyTimeLog.

Purpose: Delivers CAL-01 (post-connect picker), CAL-03 (settings editing), and CAL-04 (default push target selection).
Output: CalendarPreferencesPanel component, updated Settings page, updated DailyTimeLog with setup prompt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-calendar-preferences/01-RESEARCH.md
@.planning/phases/01-calendar-preferences/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/googleCalendar.ts:
```typescript
export interface CalendarListEntry {
  id: string
  summary: string
  backgroundColor?: string
  foregroundColor?: string
  primary?: boolean
  accessRole: 'freeBusyReader' | 'reader' | 'writer' | 'owner'
}
export async function listCalendars(token: string): Promise<CalendarListEntry[]>
```

From src/lib/useCalendarPreferences.ts:
```typescript
export function useCalendarPreferences(): {
  selectedIds: string[]
  defaultPushId: string | null
  hasConfigured: boolean
  savePreferences: (ids: string[], pushId: string | null) => Promise<void>
}
```

From src/lib/useGoogleCalendarToken.ts:
```typescript
export function getCachedCalendarToken(): string | null
export function requestCalendarToken(): Promise<string>
export function preloadGIS(): void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CalendarPreferencesPanel component + Settings page integration</name>
  <files>src/components/CalendarPreferencesPanel.tsx, src/app/settings/page.tsx</files>
  <read_first>
    - src/app/settings/page.tsx (full file — existing card pattern for Categories and Roles sections)
    - src/lib/useCalendarPreferences.ts (hook API from Plan 01)
    - src/lib/googleCalendar.ts (CalendarListEntry type and listCalendars from Plan 01)
    - src/lib/useGoogleCalendarToken.ts (getCachedCalendarToken, preloadGIS exports)
    - src/components/DailyTimeLog.tsx (first 30 lines — imports and prop types for context)
  </read_first>
  <action>
1. Create `src/components/CalendarPreferencesPanel.tsx` — a `'use client'` component:

Props: `{ token: string | null }` — if null, show "Connect Google Calendar first" message.

Behavior:
- On mount, if `token` is non-null, call `listCalendars(token)` and store results in state
- Show loading spinner while fetching (use a simple `Loading calendars...` text)
- Render each calendar as a row with:
  - A checkbox for display selection (toggles inclusion in `selectedIds`)
  - Calendar name (`summary`) with a color dot using `backgroundColor` from the API
  - A radio button for default push target — ONLY for calendars where `accessRole` is `'writer'` or `'owner'`
- Read initial state from `useCalendarPreferences()` hook (`selectedIds`, `defaultPushId`)
- On "Save" button click, call `savePreferences(localSelectedIds, localDefaultPushId)`
- Show a "Saved!" confirmation text for 2 seconds after successful save
- If `token` is null, render: "Connect Google Calendar from the Dashboard to manage calendar preferences." with a link to `/dashboard`

Styling: Use Tailwind classes matching the existing Settings page card pattern (white bg, rounded-xl, shadow, p-6). Use Lucide `CalendarDays` icon in the section header.

2. Update `src/app/settings/page.tsx`:
- Add import: `import CalendarPreferencesPanel from '@/components/CalendarPreferencesPanel'`
- Add import: `import { getCachedCalendarToken } from '@/lib/useGoogleCalendarToken'`
- Add state: `const [calToken, setCalToken] = useState<string | null>(null)`
- Add useEffect on mount: `setCalToken(getCachedCalendarToken())`
- Add a new card section BEFORE the Categories section with heading "Google Calendar" and render `<CalendarPreferencesPanel token={calToken} />`
  </action>
  <verify>
    <automated>cd /Users/garypope/Projects/life-planner && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/CalendarPreferencesPanel.tsx` exists with `'use client'` directive
    - `src/components/CalendarPreferencesPanel.tsx` imports `listCalendars` and `CalendarListEntry` from `@/lib/googleCalendar`
    - `src/components/CalendarPreferencesPanel.tsx` imports `useCalendarPreferences` from `@/lib/useCalendarPreferences`
    - `src/components/CalendarPreferencesPanel.tsx` contains `accessRole` check filtering push-target radio to `writer` or `owner`
    - `src/components/CalendarPreferencesPanel.tsx` contains `savePreferences` call
    - `src/app/settings/page.tsx` imports `CalendarPreferencesPanel`
    - `src/app/settings/page.tsx` imports `getCachedCalendarToken`
    - `src/app/settings/page.tsx` renders `<CalendarPreferencesPanel`
    - `npx tsc --noEmit` produces no errors
  </acceptance_criteria>
  <done>CalendarPreferencesPanel renders calendar list with checkboxes and radio buttons. Settings page shows Google Calendar section with the panel. Writable calendars show push-target radio. Save persists to Supabase via hook.</done>
</task>

<task type="auto">
  <name>Task 2: Post-connect setup prompt in DailyTimeLog</name>
  <files>src/components/DailyTimeLog.tsx</files>
  <read_first>
    - src/components/DailyTimeLog.tsx (full file — understand existing props, rendering, and where calendar connect button lives)
    - src/lib/useCalendarPreferences.ts (hasConfigured flag)
  </read_first>
  <action>
After `handleReconnectCalendar` successfully resolves in the dashboard (which calls `onReconnect` prop in DailyTimeLog), the user needs to know they should configure their calendars.

In `src/components/DailyTimeLog.tsx`:

1. Import `useCalendarPreferences` from `@/lib/useCalendarPreferences`
2. Import `CalendarDays` from `lucide-react` (if not already imported)
3. Call `const { hasConfigured } = useCalendarPreferences()` inside the component
4. When `tokenExpired` is false (user is connected) AND `hasConfigured` is false, render a banner:
   - Yellow/amber background card (`bg-amber-50 border border-amber-200 rounded-lg p-4`)
   - Icon: CalendarDays
   - Text: "Set up your calendar preferences to choose which calendars to display and where to push tasks."
   - A link to `/settings` styled as a button: "Configure in Settings"
5. Place this banner ABOVE the calendar events list but BELOW the reconnect button area
6. When `hasConfigured` is true, do NOT show the banner
  </action>
  <verify>
    <automated>cd /Users/garypope/Projects/life-planner && grep -q 'hasConfigured' src/components/DailyTimeLog.tsx && grep -q 'useCalendarPreferences' src/components/DailyTimeLog.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/DailyTimeLog.tsx` imports `useCalendarPreferences` from `@/lib/useCalendarPreferences`
    - `src/components/DailyTimeLog.tsx` contains `hasConfigured` variable
    - `src/components/DailyTimeLog.tsx` contains conditional rendering checking `!hasConfigured`
    - `src/components/DailyTimeLog.tsx` contains link or anchor to `/settings`
    - `src/components/DailyTimeLog.tsx` contains `bg-amber-50` or similar amber banner styling
    - `npx tsc --noEmit` produces no errors
  </acceptance_criteria>
  <done>Connected users without saved preferences see an amber banner directing them to Settings. Banner disappears once preferences are saved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete calendar preferences flow</name>
  <files>src/components/CalendarPreferencesPanel.tsx, src/app/settings/page.tsx, src/components/DailyTimeLog.tsx</files>
  <action>Human verifies the end-to-end calendar preferences flow works correctly.</action>
  <what-built>
Complete calendar preferences flow: Settings page calendar section with multi-select display picker and default push target radio buttons, plus a post-connect prompt banner in DailyTimeLog.
  </what-built>
  <how-to-verify>
Prerequisites: Run the SQL from `supabase/add_calendar_preferences.sql` in your Supabase SQL Editor first.

1. Open the app and navigate to Dashboard
2. Connect Google Calendar (if not already connected)
3. Verify: An amber banner appears saying "Set up your calendar preferences..." with a "Configure in Settings" link
4. Click "Configure in Settings" — should navigate to /settings
5. Verify: Settings page has a "Google Calendar" card section at the top
6. Verify: Your Google Calendars are listed with checkboxes and names
7. Check some calendars, select a writable calendar as push target via radio button
8. Click Save — verify "Saved!" confirmation appears
9. Reload the page — verify selections are preserved
10. Navigate back to Dashboard — verify the amber banner is GONE (preferences now configured)
11. Return to Settings — verify you can change selections and re-save
  </how-to-verify>
  <verify>Human confirms all 11 verification steps pass</verify>
  <done>All verification steps pass. Calendar preferences flow works end-to-end.</done>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- Settings page shows Google Calendar section with CalendarPreferencesPanel
- CalendarPreferencesPanel lists real Google Calendars with checkboxes and radio buttons
- Read-only calendars (reader/freeBusyReader) do not show push-target radio
- Saving preferences persists to Supabase (survives reload)
- DailyTimeLog shows setup banner when connected but unconfigured
- DailyTimeLog hides banner after preferences are saved
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
All four requirements met: CAL-01 (post-connect list shown via banner + Settings), CAL-02 (Supabase persistence survives reload), CAL-03 (Settings page editing), CAL-04 (default push target radio). User can complete full flow: connect -> configure -> save -> reload -> edit.
</success_criteria>

<output>
After completion, create `.planning/phases/01-calendar-preferences/01-02-SUMMARY.md`
</output>
